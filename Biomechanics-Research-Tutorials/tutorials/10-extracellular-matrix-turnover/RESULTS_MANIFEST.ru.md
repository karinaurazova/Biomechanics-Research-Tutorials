# Манифест результатов Tutorial 10

Каждый PNG/GIF создаётся одноимённым скриптом из `experiments/`. CSV benchmark формируется `experiments/benchmark_summary.py`. Для полного воспроизведения используйте `python reproduce.py` в каталоге tutorial или команду из README.
