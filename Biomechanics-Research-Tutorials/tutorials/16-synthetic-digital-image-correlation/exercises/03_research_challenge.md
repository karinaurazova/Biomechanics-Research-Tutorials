# Research challenge

Implement one extension: an image pyramid for large motion, a discontinuous subset model, a global finite-element DIC formulation, stereo projection, or Bayesian uncertainty propagation. Verify it on exact synthetic fields before applying it to any external image.
