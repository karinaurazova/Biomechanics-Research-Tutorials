# Research Challenge

Extend the generator with one architecture not already implemented: bundled branching trees, a periodic weave, a nonstationary pore-size field, a 3-D layered orientation gradient, or a crosslink-aware graph. Define a latent representation, a raster/voxel observation, synchronized ground truth, at least five automated verification tests, and one failure case that exposes a limitation of a standard image-analysis method.
