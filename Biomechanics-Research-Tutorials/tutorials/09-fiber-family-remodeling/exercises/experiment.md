# Experiment

Design a two-stage loading protocol and compare three models:

- direct family rotation;
- ODF drift–diffusion;
- cohort replacement.

Calibrate the models to have similar final mean orientation. Then compare transient orientation, order parameter, total mass, cohort age, and response to a third unseen loading direction. Document which hidden variables make the models distinguishable.
