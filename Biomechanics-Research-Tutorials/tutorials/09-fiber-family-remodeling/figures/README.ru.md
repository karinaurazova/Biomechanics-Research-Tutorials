# Происхождение рисунков

Каждый PNG создаётся скриптом из `../experiments/` и пересоздаётся командой:

```bash
python tutorials/09-fiber-family-remodeling/reproduce.py
```

Английские и русские версии используют одинаковые численные данные.
