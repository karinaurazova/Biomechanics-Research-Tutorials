# Figure provenance

Every PNG in this directory is generated by a script in `../experiments/` and can be regenerated with:

```bash
python tutorials/09-fiber-family-remodeling/reproduce.py
```

English and Russian figures use identical numerical data and localized labels.
