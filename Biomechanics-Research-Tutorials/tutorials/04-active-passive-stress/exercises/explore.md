# Explore

1. Run the notebook without changing parameters.
2. Increase the decay time of the activation waveform and describe the stress transient.
3. Move the optimal force–length stretch from 1.10 to 1.00.
4. Confirm numerically that `det(Fa)=1`.
5. Regenerate all figures with `reproduce.py`.
