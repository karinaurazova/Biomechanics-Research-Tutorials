# Exercise 01 — Explore the baseline calibration

Run `reproduce.py` and inspect `data/parameter_estimates.csv`.

Questions:

1. Which workflow gives the smallest parameter error?
2. Which parameter is most difficult to recover from global forces alone?
3. How do the posterior intervals compare with the deterministic least-squares estimates?
4. Which figure best explains why image-informed structure matters mechanically?

Deliverable: a short paragraph explaining whether the image-informed model is better judged by image metrics, force residuals or parameter uncertainty.
