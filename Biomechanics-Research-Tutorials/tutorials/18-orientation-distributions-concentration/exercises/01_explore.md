# Explore

Run `reproduce.py`, open `figures/odf_comparison.png`, and identify which modality gives the closest ODF to ground truth. Then inspect `data/orientation_benchmark.csv` and compare your visual judgment with Jensen-Shannon divergence.
