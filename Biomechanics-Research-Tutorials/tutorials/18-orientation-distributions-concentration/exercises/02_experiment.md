# Experiment

Change the `concentration` and `pore_count` arguments in `build_synthetic_orientation_dataset`. Regenerate the benchmark and record how `R`, `kappa`, entropy and orientation MAE change.
