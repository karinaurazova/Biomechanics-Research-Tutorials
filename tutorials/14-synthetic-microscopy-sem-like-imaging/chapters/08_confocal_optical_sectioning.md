# Confocal-like optical sectioning

A three-dimensional anisotropic PSF is used to generate a confocal-like stack. A simplified pinhole-rejection term increases the weight of locally focused signal but does not create perfect sectioning. The axial-resolution experiment shows that two nearby planes merge as $\sigma_z$ grows, even when lateral resolution is unchanged.
