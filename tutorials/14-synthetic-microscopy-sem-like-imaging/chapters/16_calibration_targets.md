# Synthetic calibration targets

Point emitters, line pairs, intensity wedges, and known 3-D objects are generated separately from tissue scenes. They support PSF estimation, resolution checks, linearity tests, and registration verification. A method should pass simple calibration targets before it is trusted on complex fibrous networks.
