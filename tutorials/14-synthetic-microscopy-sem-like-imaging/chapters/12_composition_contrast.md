# Composition contrast and confounding

An independent composition field is added to topographic contrast. The example deliberately creates two families with different composition-like values. A single grayscale image then combines material and geometric information, so segmentation accuracy alone cannot prove correct phase identification.
