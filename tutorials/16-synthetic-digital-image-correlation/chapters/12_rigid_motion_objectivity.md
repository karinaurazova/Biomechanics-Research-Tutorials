# Rigid motion and strain objectivity

A pure translation must produce zero strain. A finite rigid rotation should also produce zero Green–Lagrange strain, although the infinitesimal strain approximation can contain rotation-dependent artifacts when angles are no longer small.

These tests are essential sign and convention checks. They reveal swapped coordinate axes, incorrect image-origin handling, and confusion between engineering shear and tensor shear.
