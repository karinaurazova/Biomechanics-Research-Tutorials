# Animation provenance

`integration_scale.gif` is regenerated by `experiments/integration_scale_animation.py` through `reproduce.py`. It visualizes the same noisy two-domain image while the integration scale changes.
