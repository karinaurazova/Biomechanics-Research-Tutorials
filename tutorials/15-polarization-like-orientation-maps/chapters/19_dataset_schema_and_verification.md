# Dataset schema and verification hierarchy

The exported NPZ contains angular states, noisy observations, noise-free expectations, orientation, retardance, amplitude, mask, and JSON provenance. Unit tests verify algebraic identities, benchmark cases verify end-to-end recovery, and future experiments would be required for validation.
