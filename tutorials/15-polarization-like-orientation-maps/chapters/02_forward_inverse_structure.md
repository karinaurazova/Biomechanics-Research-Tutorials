# Forward and inverse problems

The forward problem maps orientation, retardance, amplitude, and acquisition angle to intensity. The inverse problem estimates a reduced set of optical parameters from a finite noisy angular series. Verification is possible because both the latent fields and the observations are generated computationally.
